# FTP-хостинг отключён. Сайт публикуется через GitHub Pages (.github/workflows/pages.yml).
name: Deploy to FTP Hosting (disabled)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "FTP deploy is disabled. Use pages.yml instead."
